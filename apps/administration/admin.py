"""
admin.py - Admin configuration for the Administration app.

Currently empty because this app doesn't have its own models.
It manages models from other apps (accounts, courses, etc.)
which are registered in THEIR own admin.py files.
"""

from django.contrib import admin
